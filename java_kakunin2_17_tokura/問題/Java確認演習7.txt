/////////////////////////////
//      Java確認演習7
//       抽象クラス
//    インターフェース
/////////////////////////////

※問題文中のプログラム要素の指定は以下のルールで記述している。

＜クラス・インターフェースの表記＞
 -----------------------------------------------------
|  [アクセス修飾子]クラス名(またはインターフェース名) |
|  例: +MyClass                                       |
 -----------------------------------------------------
※final、abstract修飾子や継承(実装)関係については別途補足

＜フィールドの表記＞
 -----------------------------------
|  [アクセス修飾子]変数名:データ型  |
|  例: -id:int                      |
 -----------------------------------
※static、final修飾子については別途補足

＜メソッドの表記＞
 -------------------------------------------------------------
|  [アクセス修飾子]メソッド名(変数名:データ型...):戻り値の型  |
|  例: +getId():int                                           |
 -------------------------------------------------------------
※static、final、abstract修飾子や発生しうる例外については別途補足

＜アクセス修飾子の表記＞
 ---------------------
| public		|  +  |
| protected		|  #  |
| [なし]		|  ~  |
| private		|  -  |
 ---------------------



問題① 抽象クラス

解答用ファイル: 
  kakunin07.question16.Main.java (実行クラス)
  kakunin07.question16.AbstractStudent.java（未作成）
  kakunin07.question16.GeneralStudent.java（一部作成済）
  kakunin07.question16.PartTimeWorker.java（一部作成済）
  kakunin07.question16.BandMember.java（一部作成済）
テスト用コード：
  kakunin07.question16.Question16Test.java

1. 以下のコードを記述しなさい。

   ・以下のクラスを作成。

       クラス				+AbstractStudent	※abstract
       フィールド1			#ORG_NAME:String	初期値:Java大学		※static final
       フィールド2			#grade:int			初期値:なし
       フィールド3			#name:String		初期値:なし
       ※Eclipse機能でフィールド3を初期化するコンストラクタ作成。
         同じコンストラクタ内で、フィールド2を値[1]で初期化。
       メソッド1			+selfIntroduction():void	※abstract

   ・GeneralStudentクラスに以下を追加。

       クラス				+GeneralStudent		※AbstractStudentを継承
       ※Eclipse機能で全フィールド(親クラス含む)を初期化するコンストラクタ作成。
       メソッド1			+selfIntroduction():void	※オーバーライド
         ・フィールドORG_NAME、grade、nameの値を以下の形式で表示。
            
            Java大学〇年、〇です。普通の学生です。

   ・PartTimeWorkerクラスに以下を追加。

       クラス				+PartTimeWorker		※AbstractStudentを継承
       ※Eclipse機能で全フィールド(親クラス含む)を初期化するコンストラクタ作成。
       メソッド1			+selfIntroduction():void	※オーバーライド
         ・フィールドORG_NAME、grade、name、jobの値を以下の形式で表示。
            
            Java大学〇年、〇です。〇してます。

   ・BandMemberクラスに以下を追加。

       クラス				+BandMember		※AbstractStudentを継承
       ※Eclipse機能で全フィールド(親クラス含む)を初期化するコンストラクタ作成。
       メソッド1			+selfIntroduction():void	※オーバーライド
         ・フィールドORG_NAME、grade、name、instrumentの値を以下の形式で表示。
            
            Java大学〇年、〇です。バンドで〇担当してます。

   ・実行クラスでAbstractStudent型の配列[students]を宣言し、同時に以下で初期化。
      ・引数[田中]で生成したGeneralStudent
      ・引数[藤田]、[コンビニでバイト]で生成したPartTimeWorker
      ・引数[山本]、[ギター]で生成したBandMember
   ・拡張for文を使って、[students]の全要素で以下を実行。
     各要素はAbstractStudent型の変数[s]に代入。
      ・selfIntroductionメソッドを呼び出し。

2. 以下のコードを記述しなさい。

   ・AbstractStudentクラスに以下を追加。
   
       メソッド2			+goToNextGrade():boolean
         ・gradeの値が[2]未満なら、以下の処理を行う。
            ・gradeをインクリメント
            ・name、gradeの値を以下の形式で表示。
            
               〇は〇年に進級しました。
            
            ・trueを返す。
         ・それ以外なら、以下の処理を行う。
            ・nameの値を以下の形式で表示。
            
               〇は卒業しました。
            
            ・falseを返す。

   ・PartTimeWorkerクラスに以下を追加。

       メソッド2			+goToNextGrade():boolean	※オーバーライド
         ・親クラスのgoToNextGradeメソッドを呼び出し、戻り値をそのまま返す。

   ・BandMemberクラスに以下を追加。

       メソッド2			+goToNextGrade():boolean	※オーバーライド
         ・親クラスのgoToNextGradeメソッドの戻り値が[true]なら、以下の処理を行う。
             ・フィールドnameの値を以下の形式で表示。
            
               〇はサークルの部長になりました。

            ・trueを返す。
         ・それ以外なら、以下の処理を行う。
            ・falseを返す。

   ・実行クラスで[1年経過...]と表示（改行必要）。
   ・拡張for文を使って、[students]の全要素で以下を実行。
     各要素はAbstractStudent型の変数[s]に代入。
      ・goToNextGradeメソッドを呼び出し。

3. 以下のコードを記述しなさい。

   ・PartTimeWorkerクラスで以下を変更。

       メソッド2			+goToNextGrade():boolean	※オーバーライド
         ・親クラスのgoToNextGradeメソッドの戻り値が[true]なら、以下の処理を行う。
            ・trueを返す。
         ・それ以外なら、以下の処理を行う。
             ・フィールドnameの値を以下の形式で表示。
            
               〇はコンビニ正社員に登用されました。

            ・falseを返す。

   ・実行クラスで[更に1年経過...]と表示（改行必要）。
   ・拡張for文を使って、[students]の全要素で以下を実行。
     各要素はAbstractStudent型の変数[s]に代入。
      ・goToNextGradeメソッドを呼び出し。

【1.～3.の出力例】
Java大学1年、田中です。普通の学生です。
Java大学1年、藤田です。コンビニでバイトしてます。
Java大学1年、山本です。バンドでギター担当してます。
1年経過...
田中は2年に進級しました。
藤田は2年に進級しました。
山本は2年に進級しました。
山本はサークルの部長になりました。
更に1年経過...
田中は卒業しました。
藤田は卒業しました。
藤田はコンビニ正社員に登用されました。
山本は卒業しました。



問題② インターフェース

解答用ファイル: 
  kakunin07.question17.Main.java (実行クラス)
  kakunin07.question17.StudentInterface.java（未作成）
  kakunin07.question17.GeneralStudent.java（一部作成済）
  kakunin07.question17.PartTimeWorker.java（一部作成済）
  kakunin07.question17.BandMember.java（一部作成済）
テスト用コード：
  kakunin07.question17.Question17Test.java


1. 以下のコードを記述しなさい。

   ・以下のインターフェースを作成。

       インターフェース		+StudentInterface
       フィールド1			~ORG_NAME:String	初期値:Java大学
       メソッド1			~selfIntroduction():void

   ・GeneralStudentクラスに以下を追加。

       クラス				+GeneralStudent		※StudentInterfaceを実装
       ※Eclipse機能でnameフィールドを初期化するコンストラクタ作成。
         同じコンストラクタ内で、gradeフィールドを値[1]で初期化。
       メソッド1			+selfIntroduction():void	※オーバーライド
         ・フィールドORG_NAME、grade、nameの値を以下の形式で表示。
            
            Java大学〇年、〇です。普通の学生です。

       ※Eclipse機能でフィールドgrade、name表示用の
         toStringメソッドをオーバーライド。

   ・PartTimeWorkerクラスに以下を追加。

       クラス				+PartTimeWorker		※GeneralStudentを継承
       ※Eclipse機能で全フィールド(親クラス含む)を初期化するコンストラクタ作成。
       メソッド1			+selfIntroduction():void	※オーバーライド
         ・フィールドORG_NAME、grade、name、jobの値を以下の形式で表示。
            
            Java大学〇年、〇です。〇してます。

       ※Eclipse機能でフィールドjob、grade、name表示用の
         toStringメソッドをオーバーライド。

   ・BandMemberクラスに以下を追加。

       クラス				+BandMember		※GeneralStudentを継承
       ※Eclipse機能で全フィールド(親クラス含む)を初期化するコンストラクタ作成。
       メソッド1			+selfIntroduction():void	※オーバーライド
         ・フィールドORG_NAME、grade、name、instrumentの値を以下の形式で表示。
            
            Java大学〇年、〇です。バンドで〇担当してます。

       ※Eclipse機能でフィールドinstrument、grade、name表示用の
         toStringメソッドをオーバーライド。

   ・実行クラスでStudentInterface型の配列[students]を宣言し、同時に以下で初期化。
      ・引数[田中]で生成したGeneralStudent
      ・引数[藤田]、[コンビニでバイト]で生成したPartTimeWorker
      ・引数[山本]、[ギター]で生成したBandMember
   ・拡張for文を使って、[students]の全要素で以下を実行。
     各要素はStudentInterface型の変数[s]に代入。
      ・[s]をSystem.out.println()で表示。

2. 以下のコードを記述しなさい。

   ・拡張for文を使って、[students]の全要素で以下を実行。
     各要素はStudentInterface型の変数[s]に代入。
      ・[s]のselfIntroductionメソッドを呼び出し。

3. 以下のコードを記述しなさい。

   ・GeneralStudentクラスに以下を追加。

       メソッド3			+study():void	※final
         ・Randomクラスを生成し、Random型の[r]に代入。 ※java.util.Random ランダムな値を生成するクラス
         ・[r]のnextBooleanメソッドの戻り値が[true]なら以下を実行。
            ・フィールドnameの値を以下の形式で表示。
            
               今日、〇は選択科目の日です。
            
         ・それ以外なら、以下を実行。
            ・フィールドnameの値を以下の形式で表示。
            
               今日、〇はゼミがあります。
            
   ・PartTimeWorkerクラスに以下を追加。

       メソッド3			+partTimeWork():void
         ・Randomクラスを生成し、Random型の[r]に代入。 ※java.util.Random ランダムな値を生成するクラス
         ・[r]のnextBooleanメソッドの戻り値が[true]なら以下を実行。
            ・フィールドnameの値を以下の形式で表示。
            
               今晩、〇は夜勤です。
            
         ・それ以外なら、以下を実行。
            ・フィールドnameの値を以下の形式で表示。
            
               〇は今日シフトに入ってません。
            
   ・BandMemberクラスに以下を追加。

       メソッド3			+bandActivities():void
         ・Randomクラスを生成し、Random型の[r]に代入。 ※java.util.Random ランダムな値を生成するクラス
         ・[r]のnextBooleanメソッドの戻り値が[true]なら以下を実行。
            ・フィールドnameの値を以下の形式で表示。
            
               〇は今晩、スタジオ練習日です。
            
         ・それ以外なら、以下を実行。
            ・フィールドnameの値を以下の形式で表示。
            
               〇は今晩、ライブに出ます。
            
   ・実行クラスで以下を実行。
   
       ・[students]の1番目の要素をキャストしてGeneralStudent型の[s1]に代入。
       ・[students]の1番目の要素をキャストしてPartTimeWorker型の[s2]に代入。
       ・[students]の1番目の要素をキャストしてBandMember型の[s3]に代入。
       ・[s1]のstudyメソッドを呼び出し。
       ・[s2]のpartTimeWorkメソッドを呼び出し。
       ・[s3]のbandActivitiesメソッドを呼び出し。

【1.～3.の出力例】
GeneralStudent [grade=1, name=田中]
PartTimeWorker [job=コンビニでバイト, grade=1, name=藤田]
BandMember [instrument=ギター, grade=1, name=山本]
Java大学1年、田中です。普通の学生です。
Java大学1年、藤田です。コンビニでバイトしてます。
Java大学1年、山本です。バンドでギター担当してます。
今日、田中は選択科目の日です。
今晩、藤田は夜勤です。
山本は今晩、ライブに出ます。

